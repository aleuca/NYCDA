html
    head
        meta(charset="utf-8")
        script(src='clienta.js' type="text/javascript" async defer)
    body
        h1='hello'
        each message in messages
            p=message.text
            getMessages()
        input(type='text', id='msg')
        button(type='button', id='submitButton') Send Message